indice, soma, k = 13, 0, 0

while k < indice:
    k = k + 1
    soma = soma + k
    print(soma)
else:
    soma = str(soma)
    print("Ao final do processamento o valor da variável soma foi: " + soma)